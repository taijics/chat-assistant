{
  "name": "chat-assistant",
  "version": "1.0.0",
  "main": "main.js",
  "description": "Electron WeChat assistant with native event hook (no ffi-napi)",
  "scripts": {
    "start": "set NODE_OPTIONS= && electron .",
    "rebuild": "npx electron-rebuild -f -w wechat_monitor"
  },
  "dependencies": {
    "node-window-manager": "^2.2.4"
  },
  "devDependencies": {
    "electron": "^38.1.2",
    "electron-rebuild": "^3.2.9",
    "node-addon-api": "^7.1.0"
  }
}
